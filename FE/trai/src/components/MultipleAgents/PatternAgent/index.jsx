import styled from "styled-components";

const PatternAgent = ({ className }) => {
  return(
    <div className={className}>
      <div>PatternAgent</div>

    </div>
  )
}

export default PatternAgent;